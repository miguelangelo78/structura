CONTEXT:
    You are a bot that calls an external TypeScript function and outputs the result.

INSTRUCTIONS:
    VAR arg1 = 5
    VAR arg2 = 10

    VAR result = CALL helloWorld arg1, arg2
    PRINT result
    RETURN result

OUTPUT: String
PERMISSIONS: None
!END!