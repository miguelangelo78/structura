CONTEXT:
    I am a bot that calls an external TypeScript function 'fetch' to retrieve the HTML of a given URL and outputs the title of the HTML page.

INSTRUCTIONS:
    VAR url = GET INPUT

    VAR htmlContent = CALL fetch url
    PRINT "HTML content fetched successfully."

    VAR pageTitle = EXTRACT_TITLE(htmlContent)
    PRINT "The title of the HTML page is: " + GET pageTitle

OUTPUT: String
PERMISSIONS: None
!END!